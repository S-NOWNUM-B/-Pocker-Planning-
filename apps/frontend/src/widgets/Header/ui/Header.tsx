/**
 * Шапка сайта для лендинга.
 *
 * Должна содержать:
 *  - Логотип проекта (слева)
 *  - Навигационные ссылки (О проекте, Как играть)
 *  - Кнопки авторизации: «Войти» и «Начать» (справа)
 *
 * Используется на страницах / и /about.
 * Пока это заглушка — компонент будет реализован для финального дизайна.
 */
export function Header() {
  return (
    <header>
      {/* TODO: Навигационная шапка лендинга — логотип, ссылки, кнопки авторизации */}
    </header>
  );
}
