/**
 * Универсальная форма авторизации (вход и регистрация).
 *
 * Должна содержать:
 *  - Переключатель между режимами «Вход» и «Регистрация»
 *  - Поле email с валидацией
 *  - Поле пароля с валидацией
 *  - При регистрации — дополнительно поля «Имя» и «Подтверждение пароля»
 *  - Отображение ошибок от сервера
 *  - Кнопку отправки с состоянием загрузки
 *
 * После успешного входа/регистрации вызывает authApi.login() или authApi.register(),
 * сохраняет токены через SessionManager и перенаправляет на /dashboard.
 *
 * Пока это заглушка — форма будет реализована при подключении Auth API.
 */
export function AuthForm() {
  return (
    <form>
      {/* TODO: Переиспользуемая форма авторизации — email, пароль, кнопка отправки, показ ошибок */}
    </form>
  );
}
