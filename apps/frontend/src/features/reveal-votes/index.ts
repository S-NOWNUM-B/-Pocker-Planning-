/**
 * Экспорт фичи раскрытия голосов.
 *
 * RevealButton — кнопка раскрытия результатов.
 * useRevealVotes — хук для вызова POST /rooms/:roomId/reveal.
 */
export { RevealButton } from './ui/RevealButton';
export { useRevealVotes } from './lib/useRevealVotes';
