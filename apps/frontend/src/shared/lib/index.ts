/**
 * Экспорт утилит слоя shared/lib.
 *
 * cn — условное объединение CSS-классов.
 * useLocalStorage, useWebSocket — пользовательские хуки.
 */
export { cn } from './cn';
export { useLocalStorage, useWebSocket } from './hooks';
