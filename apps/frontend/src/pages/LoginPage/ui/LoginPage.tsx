/**
 * Страница входа в аккаунт.
 *
 * Должна содержать форму с полями email и пароль,
 * кнопкой «Войти» и ссылкой на страницу регистрации.
 *
 * После успешного входа:
 *  - Сохранить JWT-токены через SessionManager
 *  - Перенаправить пользователя на /dashboard
 *  - При ошибке — показать сообщение под формой
 */
export function LoginPage() {
  return (
    <div>
      <h1>Login</h1>
      {/* TODO: Реализовать форму входа: email, пароль, кнопка «Войти» */}
    </div>
  );
}
