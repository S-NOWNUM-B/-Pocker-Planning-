/**
 * Страница регистрации нового пользователя.
 *
 * Должна содержать форму с полями: имя, email, пароль, подтверждение пароля.
 * Валидация через Zod-схему:
 *  - email — корректный формат
 *  - пароль — минимум 8 символов, буквы + цифры
 *  - подтверждение — совпадает с паролем
 *
 * После успешной регистрации:
 *  - Сохранить JWT-токены через SessionManager
 *  - Перенаправить на /dashboard
 *  - При ошибке (email уже занят) — показать сообщение
 */
export function RegisterPage() {
  return (
    <div>
      <h1>Register</h1>
      {/* TODO: Реализовать форму регистрации: имя, email, пароль, подтверждение */}
    </div>
  );
}
